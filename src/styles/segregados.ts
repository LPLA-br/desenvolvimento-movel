import { StyleSheet } from "react-native";
import Colors from "../constants/Colors";

/** Estilizacao de botões com ação relativa à operações
* matemáticas.*/
const estiloReservado = StyleSheet.create(
{
  botaoSegregado:
  {
    backgroundColor: Colors.vermelho
  }
});

export default estiloReservado;
